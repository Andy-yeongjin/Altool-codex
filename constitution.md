# 프로젝트 개발 헌법 (Project Development Constitution)

**Version**: 3.1.0 | **Ratified**: 2026-02-26 | **Last Amended**: 2026-09-08

## Sync Impact Report

`3.0.1 → 3.1.0`: 회사 공통 팩에서 채택한 의미 자산의 필수 재사용 경계를 명확히 한다. 사용자 디자인의 화면 구조 권위는 유지하되 회사 아이콘·문구 등의 변경을 기능별 암묵적 예외로 처리하지 않는다. 구체적 ID·변형·배포·검증 절차는 표준과 자산 팩이 소유한다.

`3.0.0 → 3.0.1`: 디자인 원본 위치 표기를 하위 폴더 HTML까지 명확히 정정한다. 권위·소유권·품질 원칙은 변경하지 않는다.

`2.0.1 → 3.0.0`: 헌법은 Altool 제품이 관리하는 판단 원칙, standards는 AI가 유지하는 프로젝트 공통 계약, PRD/Spec은 기능 범위와 구현 결정을 소유합니다. 디자인 계약은 `standards/design.md`로 이동하고 원본은 `designs/`에 보존합니다. 설치·업데이트는 기존 헌법이 다르면 백업 후 제품판으로 교체하고 기존 표준은 보존합니다. 기존 AGENTS의 라우터 진입 규칙은 업그레이드 시 검토합니다.

## 핵심 원칙

### 제1조: 적용 범위·언어·시간대

- 이 헌법은 설치 프로젝트의 방법론, 기술 기본값, 디자인 권위와 품질 기준을 정의합니다. `AGENTS.md`는 에이전트 행동과 UI 도구 조작, `altool/steps/`는 명령 절차만 담당합니다. 기능 범위는 사용자 지시와 승인된 PRD를 따르며 문서 충돌은 숨기지 않고 권위 원천에 맞춰 동기화합니다.
- 반복 적용 계약은 `standards/standard.yaml`이 지정하는 활성 표준 원문을 따른다. 필수 계약·기본값·예시를 구분하며 구체적 요구는 기본값을 대체할 수 있지만 필수 계약을 암묵적으로 면제하지 않는다. 비적용과 예외는 구분하여 Plan/Spec 결정 기록에 근거를 남긴다. 작업별 선택과 증거 절차는 `altool/standards.md`가 소유한다.
- 계획·명세·분석·보고 등 표준 문서는 한글로 작성합니다. 코드 식별자와 외부 프로토콜은 생태계 관례를 따르되 도메인 용어의 의미를 유지합니다.
- 타임스탬프 저장·전송은 timezone-aware UTC, 사용자 표시와 대한민국 비즈니스 날짜는 `Asia/Seoul`을 기본으로 합니다. 날짜는 `YYYY-MM-DD`, 날짜·시간은 offset을 포함한 ISO 8601을 사용하며 외부 계약이 다르면 변환 경계를 명시합니다.

### 제2조: SDD·DDD·ADR

- 동작, 공개 계약, 데이터 모델, 사용자 경험을 바꾸기 전에 관련 Plan과 Spec을 작성하거나 갱신합니다. 동작을 바꾸지 않는 작은 문서·코드 정리에는 형식적인 산출물을 만들지 않습니다.
- 표준 흐름은 `Research → Plan → Spec → Run → Analyze → Fix → Browser(UI 변경 시) → Report`입니다. PRD는 범위 계약, Research는 근거 보강이며 Plan과 Spec의 기본 경로는 각각 `docs/01-plan/features/*.plan.md`, `docs/02-spec/features/*.spec.md`입니다.
- 요구사항, 성공 기준, 구현, 테스트, 검증 증거를 추적 가능하게 유지합니다. 설계가 틀렸다면 문서를 먼저 고친 뒤 구현과 상태를 동기화합니다.
- 기획·명세·코드·데이터는 같은 도메인 용어를 사용하고, 분리 가치가 있는 핵심 규칙만 UI·프레임워크·DB와 격리합니다. 실제 경계나 변경 가능성이 없으면 Bounded Context, 플러그인 구조, 추상화를 만들지 않습니다.
- 중요하고 되돌리기 어려운 기술·데이터·비즈니스 결정만 ADR 또는 Plan·Spec 결정 기록에 이유와 대안을 남깁니다.

### 제3조: 기술 스택 기본값

기존 스택, 사용자·PRD 요구, 배포 환경과 문제 규모를 우선합니다. 새 프로젝트에 다른 근거가 없을 때만 다음 기본값을 적용합니다.

| 영역 | 기본값 | 조건 |
|------|--------|------|
| 동적 웹앱 | Next.js App Router + TypeScript Strict | 일반 웹 애플리케이션 |
| 정적 결과물 | HTML/CSS/JavaScript | 프레임워크가 불필요할 때 |
| DB | SQLite 로컬 → 배포 DB는 ADR | 영속 데이터가 필요할 때 |
| ORM | Prisma 또는 Drizzle | ORM 이점이 있을 때 |
| 스타일 | 기존 방식, 신규는 CSS Modules 우선 | 디자인 원천의 값을 사용 |
| 인증 | Auth.js 또는 적합한 세션 관리 | 보호 기능이 있을 때 |

- React/Next.js에서만 `Vercel React Best Practices`를 적용하고, 기존 프로젝트를 요청 없이 마이그레이션하지 않습니다.
- 요구가 없는 DB, ORM, 인증, 플러그인 구조는 추가하지 않습니다.

### 제4조: 디자인 권위·UI 품질

| 우선순위 | 원천 | 역할 |
|------|------|------|
| 1 | `designs/claude-design/**/*.html` | 존재할 때 구조·밀도·위계·컴포넌트·상태의 최상위 원천 |
| 2 | `.pen`, Stitch, 스크린샷, 디자인 문서 | 기타 사용자 디자인 원본 |
| 3 | `standards/design.md` | 원본을 정규화한 구현 계약 |
| 4 | Research | 상위 원천의 빈칸 보강 |
| 5 | AI 판단 | 최후의 세부 보완 |

- 직접 원천이 새롭거나 `design.md`와 충돌하면 구현 전 `design_source` 또는 Research에서 동기화합니다. Claude HTML이 있으면 HTML을 우선하고, 없으면 완성된 `design.md`를 단일 구현 계약으로 사용합니다. 같은 화면의 Claude HTML끼리 충돌하면 권위 기준을 확인합니다.
- 위 우선순위는 시각 계약에 적용하며 인증·권한·데이터 및 채택된 회사 공통 의미 자산의 필수 계약을 덮어쓰지 않습니다. 같은 의미의 아이콘·문구·컴포넌트는 공통 팩을 재사용하고 충돌은 공통 변경의 영향·승인 범위로 해결합니다. 디자인 원본은 designs/, 정규화된 토큰·컴포넌트·화면 계약은 standards/design.md에 둡니다.
- CSS 변수와 컴포넌트 상수는 파생값일 뿐 별도 원천이 아닙니다. 참조 사이트의 문구, 로고, 고유 이미지, 브랜드 자산, 라이선스 불명 폰트는 복제하지 않습니다.
- 모든 UI는 충분한 대비·위계·조작 영역·포커스, 의미 있는 HTML과 보조기술 접근, 명확한 로딩·빈·오류·성공·비활성 상태를 제공합니다.
- 데스크톱과 모바일에서 겹침·잘림·조작 불가가 없어야 하며, 디자인 원천이 사용성을 해치면 의도를 보존하는 범위에서 보정하고 이유를 기록합니다.

### 제5조: 완료 품질 게이트

해당하지 않는 게이트는 억지로 구현하지 않고 `N/A` 또는 생략 근거를 남깁니다.

| 게이트 | 완료 조건 |
|--------|-----------|
| 계약 | 승인 범위와 성공 기준이 Plan·Spec·구현에 추적됨 |
| 정적·빌드 | 적용 가능한 타입·린트·빌드 통과 |
| 테스트 | 핵심 규칙, API 계약, 회귀 위험을 증명하는 테스트 통과 |
| 보안 | 비밀정보, 입력 검증, 권한 경계, 변경 의존성 확인 |
| UI | 변경 UI의 실제 브라우저 조작, 상태, 반응형, 콘솔·네트워크 확인 |
| 증거 | 실행 명령·결과·잔여 위험 기록; Altool은 Step Check와 문서 상태 동기화 통과 |

- 실패, 미해소 갭, 필수 브라우저 검증 누락을 성공으로 보고하지 않습니다. 검증할 수 없으면 차단 사유와 잔여 위험을 보고합니다.
- 구체적인 UI 조작법은 `AGENTS.md`, Altool 브라우저 산출물과 상태 절차는 `altool/steps/browser.md`를 따릅니다.

### 제6조: 보안·예외 처리·관측 가능성

- 비밀정보와 개인정보를 소스·설정·로그에 하드코딩하지 않고 환경 변수 또는 승인된 Secret Manager로 관리합니다. 인증·권한은 보호가 필요한 서버 경계에서만 검사합니다.
- 현실적인 오류는 사용자 행동 안내와 개발자 진단을 분리합니다. 운영상 필요한 흐름만 민감정보 없이 구조화해 기록하고, 불가능한 상황을 위한 방어 코드는 추측해 추가하지 않습니다.
- ACID, 재시도, 복구, 모니터링은 데이터 무결성과 장애 위험에 비례해 적용합니다.

### 제7조: 조건부 제품 기본정책

- 공개 사용자용 웹서비스는 별도 요구가 없을 때 홈 또는 핵심 진입 경험을 먼저 완성하고, 공개 콘텐츠는 탐색을 허용하되 쓰기·개인 데이터 접근 시 인증을 요구합니다.
- 관리자·사내·회원 전용 서비스, API·라이브러리 또는 다른 PRD 흐름에는 이 기본값을 적용하지 않습니다.

## 거버넌스

- 설치 프로젝트의 사용자와 AI는 이 헌법을 변경하지 않습니다. 헌법과 무결성 기준은 Altool 제품 업데이트로만 배포하며, 변경 감지 시 게이트를 우회하지 않고 설치본을 복구합니다. Altool 제품 자체를 개발하는 명시적 작업은 이 배포 원본을 수정할 수 있습니다.
- 사용자는 원하는 동작을 자연어로 설명합니다. AI는 요청 범위 안에서 공통 계약을 `standards/`에, 기능 한정 요구를 PRD/Spec에 반영하며 표준의 선택·추가·통합·갱신과 라우터 동기화를 담당합니다. 공통화 판단·영향 검증·확인이 필요한 경계는 `altool/standards.md`를 따릅니다.
- 제품 원칙의 의미나 책임 경계가 바뀌면 버전·Sync Impact Report·배포 무결성 기준을 갱신하고 README, `AGENTS.md`, Altool skill·step·template의 중복·충돌을 함께 점검합니다.
- 버전은 MAJOR(원칙 재정의), MINOR(원칙 추가·확장), PATCH(의미 없는 정정·압축)를 사용합니다.
