# 사내 시스템 공통영역 개발 표준

**버전** v3.0 (통합본)
**대상** 사내 업무 시스템 공통 기반 영역
**용도** AI 코딩 도구(Claude Code, Cursor 등)에 제공하는 개발 명세서

---

## 목차

**PART I. 프로젝트 정의**
- [1. 개요](#1-개요)
- [2. 아키텍처 4대 원칙](#2-아키텍처-4대-원칙)

**PART II. 명명 규칙**
- [3. 공통 원칙](#3-공통-원칙)
- [4. DB 명명 규칙](#4-db-명명-규칙)
- [5. 애플리케이션 명명 규칙](#5-애플리케이션-명명-규칙)
- [6. 규칙 강제 (ESLint)](#6-규칙-강제-eslint)

**PART III. 데이터 모델**
- [7. 테이블 일람과 관계](#7-테이블-일람과-관계)
- [8. 테이블 정의](#8-테이블-정의)
- [9. DDL 예시](#9-ddl-예시)

**PART IV. 기능 명세**
- [10. 인증](#10-인증)
- [11. 권한](#11-권한)
- [12. 변경이력](#12-변경이력)
- [13. 엑셀 일괄 업로드 엔진](#13-엑셀-일괄-업로드-엔진)

**PART V. 화면과 인터페이스**
- [14. 화면 목록](#14-화면-목록)
- [15. 공통 컴포넌트](#15-공통-컴포넌트)
- [16. API 규격](#16-api-규격)

**PART VI. 구현**
- [17. 구현 순서](#17-구현-순서)
- [18. 구현 프롬프트 세트](#18-구현-프롬프트-세트)
- [19. 완료 체크리스트](#19-완료-체크리스트)

**부록**
- [A. 표준 단어사전](#부록-a-표준-단어사전)
- [B. 공통코드 초기값](#부록-b-공통코드-초기값)
- [C. 참고 자료](#부록-c-참고-자료)

---

## 0. 이 문서 사용법

1. 프로젝트 루트에 `SPEC.md` 로 저장한다.
2. AI 도구에 **매 세션 시작 시 이 문서를 먼저 읽히고** 작업을 지시한다.
3. `18장 프롬프트 세트` 를 **순서대로** 진행한다. 순서를 건너뛰면 뒤에서 전면 수정이 발생한다.
4. STEP 완료 시마다 `19장 체크리스트` 로 검증한다.
5. 문서가 길어 컨텍스트 부담이 크면, **PART II(명명 규칙)만 별도 파일로 분리**해 항상 함께 물린다.

---

# PART I. 프로젝트 정의

## 1. 개요

### 1.1 목적

사내 업무 시스템의 공통 기반(프레임워크) 영역을 구축한다. 업무 화면은 이 기반 위에 얹는다.

### 1.2 전제 조건

| 항목 | 내용 |
|---|---|
| 사용자 규모 | 사내 임직원 (수백 명 수준) |
| 인증 | **SSO 없음**. 자체 ID/PW 인증 |
| 데이터 입력 | 화면 등록 + **엑셀 대량 업로드 필수** |
| 이력관리 | **전 마스터 테이블 변경이력 필수** |
| 다국어 | 미적용 (한국어 전용) |
| 결재/워크플로우 | 범위 외 |

### 1.3 기술 스택

> 스택이 다르면 이 절과 5장만 교체한다. 나머지는 그대로 사용 가능하다.

| 구분 | 선택 |
|---|---|
| Framework | Next.js 15+ (App Router) + TypeScript |
| DB | PostgreSQL |
| ORM | Prisma |
| UI | Tailwind CSS + shadcn/ui |
| Table | TanStack Table |
| Excel | SheetJS (`xlsx`) |
| Validation | Zod |
| Auth | 자체 구현 (bcrypt + 세션 쿠키) |
| Lint | ESLint + typescript-eslint |

### 1.4 범위

**포함** — 인증 · 사용자 · 조직 · 역할 · 메뉴 · 권한 · 공통코드 · 엑셀 일괄등록 · 변경이력 · 접속이력

**제외 (Phase 2 이후)** — 조직 이력 시점조회 화면 · 겸직/파견 · 대리(위임) · 버튼 단위 권한 · 컬럼 마스킹 · 권한신청 승인 프로세스 · 다국어 · 알림/메일 발송 · 인터페이스 관리 · 배치 관리 화면 · 결재 워크플로우 · 개인화 설정 · 다중 역할 · MFA

---

## 2. 아키텍처 4대 원칙

**이 프로젝트의 성패를 결정하는 네 가지다. AI에게 반복 강조한다.**

### 원칙 1 — 명명 규칙은 예외 없이 적용한다

PART II 를 준수한다. 표준 단어사전에 없는 축약어를 임의로 만들지 않는다. 규칙 위반은 6장 ESLint 로 강제한다.

### 원칙 2 — 감사로그는 ORM 훅에서 자동으로 남긴다

서비스 코드에서 감사로그 함수를 직접 호출하지 않는다. 개발자가 잊어도 남아야 한다. (→ 12장)

### 원칙 3 — 엑셀 업로드는 공통 엔진 1개다

화면마다 만들지 않는다. **엔진 1개 + 엔티티 정의(JSON)** 구조로, 화면 추가 시 정의만 작성한다. (→ 13장)

### 원칙 4 — 삭제는 전부 논리삭제다

물리 DELETE 를 쓰지 않는다. `del_yn = true` 로 처리하고 감사로그에 `action_type = 'DELETE'` 로 기록한다.

---

# PART II. 명명 규칙

## 3. 공통 원칙

### 3.1 대전제

**일관성이 정답보다 중요하다.** 팀 사정상 규칙을 바꾼다면 전체를 함께 바꾼다. **일부만 다른 것이 가장 나쁘다.**

### 3.2 계층별 표기 한눈에

| 계층 | 표기 | 예 |
|---|---|---|
| DB 테이블/컬럼 | `snake_case` | `sys_user.dept_code` |
| 폴더 | `kebab-case` | `sys-user/` |
| 파일 | `kebab-case` | `user-form.tsx` |
| React 컴포넌트 | `PascalCase` | `UserForm` |
| 타입/인터페이스/Enum | `PascalCase` | `SysUser`, `UserStatus` |
| 변수/함수/속성 | `camelCase` | `deptCode`, `getUserList()` |
| 상수 (모듈 레벨) | `UPPER_SNAKE_CASE` | `MAX_UPLOAD_ROWS` |
| React Hook | `use` + PascalCase | `useUserList` |
| API 경로 | `kebab-case` | `/api/sys-user` |
| API JSON 키 | `camelCase` | `{ "deptCode": "D001" }` |
| 쿼리 파라미터 | `camelCase` | `?deptCode=D001&page=1` |
| 환경변수 | `UPPER_SNAKE_CASE` | `DATABASE_URL` |
| CSS 클래스 | `kebab-case` | `.user-card` |
| Git 브랜치 | `kebab-case` | `feat/excel-upload-engine` |

### 3.3 변환 지점은 한 곳

DB `snake_case` ↔ 앱 `camelCase` 변환은 **ORM 매핑에서만** 한다. 서비스 코드에서 수동 변환하지 않는다.

```prisma
model SysUser {
  empNo     String   @id @map("emp_no")
  userName  String   @map("user_name")
  deptCode  String   @map("dept_code")
  useYn     Boolean  @map("use_yn")
  delYn     Boolean  @map("del_yn")
  createdAt DateTime @map("created_at")

  @@map("sys_user")
}
```

### 3.4 전 계층 공통 금지

| 금지 | 사유 |
|---|---|
| 한글 발음 영문표기 (`sawon`, `busu`) | 외부 인력 투입 시 해독 불가 |
| 모음 뺀 축약 (`usr`, `dpt`, `cd`, `nm`, `mgr`) | 복원이 사람마다 다름 |
| 숫자 접미어 (`name1`, `name2`) | 의미 불명 |
| 의미 없는 이름 (`data`, `info`, `temp`, `obj`) | 정보량 없음 |
| 동의어 혼용 (`dept` / `org`) | 검색·조인 누락 |
| 대소문자 혼용 | DBMS·OS별 처리 상이 |

---

## 4. DB 명명 규칙

### 4.1 기본

| 원칙 | 내용 |
|---|---|
| 표기 | 소문자 `snake_case` |
| 단수형 | 테이블명은 단수. `users` ✕ → `sys_user` ○ |
| 축약 | 표준 단어사전(부록 A)에 등재된 것만 |
| 예약어 | SQL 예약어 사용 금지 |
| 길이 | 테이블 30자, 컬럼 30자 이내 |

### 4.2 테이블 명명

```
[접두어]_[대상]
```

| 접두어 | 용도 | 예 |
|---|---|---|
| `sys_` | 시스템 공통 | `sys_user`, `sys_dept` |
| `log_` | 로그 (INSERT 전용) | `log_audit`, `log_login` |
| `his_` | 스냅샷 이력 | `his_dept` |
| `map_` | 다대다 매핑 | `map_role_menu` |
| `biz_` | 업무 데이터 | `biz_request` |
| `tmp_` | 임시·작업용 | `tmp_excel_stage` |

**예약어 회피** — 아래는 접두어 없이 쓰지 않는다.

```
user  role  order  group  table  column  key  index  view
comment  code  status  type  level  session  authorization
```

> `tb_`, `t_` 같은 **타입 접두어는 사용하지 않는다.** 테이블인 것은 이미 알고 있어 정보량이 없다. 여기서 쓰는 것은 성격을 구분하는 **도메인 접두어**다.

### 4.3 컬럼 명명

```
[수식어]_[대상]_[도메인 접미어]
```

**마지막 단어(접미어)가 타입과 의미를 결정한다.**

### 4.4 표준 도메인 — 접미어별 타입 고정

같은 성격 컬럼이 테이블마다 다른 길이로 정의되는 것을 막는다.

| 접미어 | 의미 | 타입 (고정) | 예 |
|---|---|---|---|
| `_id` | 시스템 생성 식별자 | `BIGSERIAL` / `UUID` | `audit_id`, `batch_id` |
| `_code` | 업무 부여 코드 | `VARCHAR(20)` | `dept_code` |
| `_no` | 번호 (앞자리 0 유지) | `VARCHAR(20)` | `emp_no` |
| `_name` | 명칭 | `VARCHAR(100)` | `user_name` |
| `_desc` | 설명 | `VARCHAR(200)` | `role_desc` |
| `_content` | 본문 | `TEXT` | `notice_content` |
| `_yn` | 참/거짓 | `BOOLEAN` | `use_yn`, `del_yn` |
| `_at` | 시각 | `TIMESTAMP` | `created_at` |
| `_date` | 날짜만 | `DATE` | `join_date` |
| `_cnt` | 건수 | `INT` | `fail_cnt` |
| `_amt` | 금액 | `DECIMAL(18,2)` | `budget_amt` |
| `_qty` | 수량 | `DECIMAL(15,3)` | `stock_qty` |
| `_rate` | 비율 | `DECIMAL(5,2)` | `progress_rate` |
| `_seq` | 정렬 순서 | `INT` | `sort_seq` |
| `_level` | 계층 레벨 | `INT` | `dept_level` |
| `_type` | 구분값 (공통코드) | `VARCHAR(20)` | `menu_type` |
| `_status` | 상태값 | `VARCHAR(20)` | `user_status` |
| `_url` | URL | `VARCHAR(200)` | `menu_url` |
| `_path` | 경로 | `VARCHAR(300)` | `file_path` |
| `_json` | JSON 구조 | `JSONB` | `before_json` |
| `_ip` | IP 주소 | `VARCHAR(45)` | `client_ip` |

**단위가 있는 값은 접미어 앞에 단위 명시.** `weight` ✕ → `weight_kg` ○

### 4.5 불리언 — `_yn` 통일

```
use_yn      ○
del_yn      ○
is_used     ✕     is_ 접두어 금지
used        ✕
```

`is_` 와 `_yn` 을 섞으면 조건문에서 지속적으로 혼동된다.

### 4.6 공통 컬럼 (전 마스터 테이블 고정)

이름·순서를 테이블마다 바꾸지 않는다. ORM 훅과 감사로그가 이 이름에 의존한다.

```sql
del_yn        BOOLEAN      NOT NULL DEFAULT false
created_by    VARCHAR(20)  NOT NULL
created_at    TIMESTAMP    NOT NULL DEFAULT now()
updated_by    VARCHAR(20)
updated_at    TIMESTAMP
```

> **`log_*` 테이블에는 넣지 않는다.** 로그는 수정·삭제 대상이 아니라 `del_yn`, `updated_at` 이 의미가 없다.

### 4.7 외래키 컬럼

**참조 대상 테이블의 PK 컬럼명을 그대로 쓴다.**

```
sys_user.dept_code  →  sys_dept.dept_code      ○
sys_user.dept       →  sys_dept.dept_code      ✕
sys_user.fk_dept    →  sys_dept.dept_code      ✕
```

같은 대상을 두 번 참조할 때만 수식어를 앞에 붙인다.

```
sys_dept.manager_emp_no      부서장
biz_request.req_emp_no       요청자
biz_request.approve_emp_no   승인자
```

### 4.8 제약조건·인덱스

| 대상 | 규칙 | 예 |
|---|---|---|
| Primary Key | `pk_{테이블}` | `pk_sys_user` |
| Foreign Key | `fk_{자}_{부}` | `fk_sys_user_dept` |
| Unique | `uk_{테이블}_{컬럼}` | `uk_sys_user_email` |
| Index | `ix_{테이블}_{용도}` | `ix_log_audit_target` |
| Check | `ck_{테이블}_{컬럼}` | `ck_sys_user_status` |

### 4.9 DB 금지 사항

| 금지 | 사유 |
|---|---|
| `tb_`, `t_` 접두어 | 정보량 없음 |
| 컬럼에 테이블명 반복 (`sys_user.user_user_name`) | 중복 |
| 범용 컬럼 (`field1`~`field20`) | 6개월 뒤 용도 불명 |
| 콤마 구분 다중값 (`tags = "A,B,C"`) | 정규화 위반. 조인·검색 불가 |
| EAV 구조 (`attr_name`/`attr_value`) | 제약·인덱스 무력화 |
| 물리삭제 | 복구·감사추적 불가 |
| 의미 있는 코드값 (`A01` = 서울영업) | 조직 변경 시 체계 붕괴 |

---

## 5. 애플리케이션 명명 규칙

### 5.1 파일·폴더 — kebab-case 통일

```
components/
  user-profile.tsx
  excel-upload-modal.tsx
lib/
  audit-context.ts
  excel-parser.ts
```

**왜 PascalCase 파일명이 위험한가** — macOS/Windows 파일시스템은 대소문자를 구분하지 않지만 Linux는 구분한다. `UserForm.tsx` 를 `userForm.tsx` 로 import 하면 로컬에서는 되고 배포 서버에서 깨진다. 실제로 자주 발생하는 사고다.

> 컴포넌트 파일만 PascalCase 로 예외를 두는 팀도 있다. 어느 쪽이든 가능하나 본 프로젝트는 **전부 kebab-case** 로 간다.

**접미어 규칙**

| 종류 | 접미어 | 예 |
|---|---|---|
| 타입 정의 | `.types.ts` | `user.types.ts` |
| 서버 액션 | `.actions.ts` | `user.actions.ts` |
| 서비스 | `.service.ts` | `user.service.ts` |
| Zod 스키마 | `.schema.ts` | `user.schema.ts` |
| 상수 | `.const.ts` | `upload.const.ts` |
| 커스텀 훅 | `use-` 접두어 | `use-user-list.ts` |
| 엑셀 엔티티 정의 | `.def.ts` | `sys-user.def.ts` |
| 테스트 | `.test.ts` | `excel-parser.test.ts` |

### 5.2 Next.js App Router 예약 파일

App Router 는 파일명 자체가 역할을 갖는 규약을 쓴다. `page.tsx`, `layout.tsx`, `loading.tsx`, `error.tsx`, `not-found.tsx`, `route.ts` 는 **이름을 바꿀 수 없다.**

```
app/
  layout.tsx                루트 레이아웃 (필수)
  page.tsx                  /
  (auth)/                   라우트 그룹 — URL에 안 나타남
    login/page.tsx          /login
  (main)/
    layout.tsx              사이드바 포함 레이아웃
    sys-user/
      page.tsx              /sys-user
      [empNo]/page.tsx      /sys-user/E001
  api/
    sys-user/route.ts       /api/sys-user
```

- 라우트 폴더는 소문자 kebab-case. **폴더명이 곧 URL**이다
- 동적 세그먼트는 `[empNo]`, `[deptCode]` — params 키가 되므로 camelCase
- 라우트 그룹 `(auth)`, `(main)` 은 URL에 나타나지 않는다

### 5.3 프로젝트 구조 — 기능 단위

기술 종류가 아니라 **기능 단위**로 묶는다. 화면 하나 고치려고 5개 폴더를 오가지 않게 된다.

```
src/
  app/                          라우팅만
  features/
    sys-user/
      components/
        user-form.tsx
        user-search-bar.tsx
      hooks/use-user-list.ts
      user.types.ts
      user.schema.ts
      user.service.ts
    excel-upload/
      excel-engine.ts
      excel-parser.ts
      excel-validator.ts
      defs/
        sys-user.def.ts
        sys-dept.def.ts
  components/ui/                shadcn 등 범용 UI
  components/common/            DataGrid, TreeView 등 공통
  lib/                          prisma, auth, audit-context
  types/                        전역 타입
```

### 5.4 TypeScript 식별자

```ts
const userName = 'kim';                    // 변수 camelCase
function getUserList() {}                  // 함수 camelCase
const MAX_UPLOAD_ROWS = 5000;              // 모듈 레벨 상수 UPPER_SNAKE

type UploadDef = { entity: string };       // 타입 PascalCase
interface SessionUser { empNo: string }    // 멤버는 camelCase
```

**인터페이스에 `I` 접두어 금지** — TypeScript 표준 라이브러리가 `Window`, `Document` 등을 접두어 없이 정의하고 있어 관례에 반한다.

```ts
interface IUser {}     ✕
interface User {}      ○
```

**`type` 기본, `interface` 는 확장이 필요한 공개 계약만.**

**타입에 이미 있는 정보를 이름에 넣지 않는다.**

```ts
const userNameString: string   ✕
const userName: string         ○
type UserType = {}             ✕
type User = {}                 ○
```

**함수 내 지역 const 는 camelCase.** 재할당하지 않는다고 UPPER_CASE 가 아니다.

```ts
const totalCount = rows.length;   ○
const TOTAL_COUNT = rows.length;  ✕
```

**상태값은 `as const` 유니온.** DB 값과 문자열이 정확히 일치해야 한다.

```ts
export const USER_STATUS = ['ACTIVE', 'INACTIVE', 'LOCKED'] as const;
export type UserStatus = typeof USER_STATUS[number];
```

### 5.5 ★ 불리언 — DB `_yn` 과 앱 `is~` 의 경계

TypeScript 관례는 `is`, `has`, `can` 접두어로 불리언을 구분하도록 권장한다. DB의 `use_yn` 과 경계에서 부딪힌다.

| 방안 | 내용 | 평가 |
|---|---|---|
| A | DTO에서 `useYn` 그대로 사용 | 매핑 1:1, 추적 쉬움 |
| B | DTO에서 `isUsed` 로 개명 | TS 관례 부합. 매핑표 필요 |
| C | DB도 `is_used` 로 통일 | 일관성 최상. 국내 관행과 충돌 |

**→ A 채택.** DB 컬럼과 TS 필드 이름이 다르면 디버깅 때마다 매핑표를 봐야 한다. 필드 수십 개 중 불리언만 다른 규칙을 쓰는 비용이 더 크다.

```ts
// DB에서 온 필드 — ~Yn 유지
type SysUser = {
  empNo: string;
  useYn: boolean;      // ← sys_user.use_yn
  delYn: boolean;
};

// 코드에서 계산한 값 — is/has/can
const isEditable = user.useYn && !user.delYn && perm.updateYn;
const hasChildDept = children.length > 0;
```

**경계는 명확하다. DB에서 온 값은 `~Yn`, 코드에서 만든 값은 `is~/has~/can~`.**

**부정형 이름은 쓰지 않는다.** `isNotActive` → `!isActive`. 이중부정을 유발한다.

### 5.6 함수 — 동사 표준

| 동사 | 의미 | 예 |
|---|---|---|
| `get` | 단순 조회 (동기/캐시) | `getDeptTree()` |
| `fetch` | 외부·DB 조회 (비동기) | `fetchUserList()` |
| `find` | 조건 검색. 없으면 null | `findUserByEmail()` |
| `create` | 생성 | `createUser()` |
| `update` | 수정 | `updateDept()` |
| `remove` | 논리삭제 | `removeUser()` |
| `delete` | 물리삭제 | **사용 금지** |
| `validate` | 검증. 오류 배열 반환 | `validateExcelRow()` |
| `parse` | 변환 | `parseExcelFile()` |
| `handle` | 이벤트 핸들러 | `handleSubmit()` |
| `on` | 핸들러 props | `onSubmit` |
| `build` / `make` | 조립 | `buildAuditDiff()` |
| `to` | 타입 변환 | `toCamelCase()` |
| `is` / `has` / `can` | 불리언 반환 | `hasPermission()` |

**`get` 과 `fetch` 를 구분하는 이유** — 함수명만으로 비동기 여부와 비용을 판단할 수 있다. 반복문 안에 `fetch~` 가 보이면 즉시 N+1 문제로 인식된다.

### 5.7 React

**컴포넌트** — PascalCase, 명사구

| 접미어 | 용도 |
|---|---|
| `~Form` | 입력 폼 |
| `~List` / `~Table` | 목록 |
| `~Modal` / `~Dialog` | 팝업 |
| `~Panel` / `~Card` | 영역 |
| `~Picker` | 선택 팝업 (`UserPicker`) |
| `~Provider` | Context 공급자 |

**Props** — `{컴포넌트명}Props`. 콜백은 `on~`, 내부 핸들러는 `handle~`

```ts
type UserFormProps = {
  empNo?: string;
  mode: 'create' | 'edit';
  disabled?: boolean;                  // UI props 는 부정형 관례 허용
  onSubmit: (v: UserCreateInput) => void;
  onCancel?: () => void;
};
```

```tsx
const handleSubmit = () => { ... };
<UserForm onSubmit={handleSubmit} />
```

**Hook** — `use` 로 시작해야 React 가 훅으로 인식한다. 관례가 아니라 **강제사항**이다.

```ts
useUserList()  useDeptTree()  useExcelUpload()  useAuditContext()
```

**State** — setter 는 `set` + 상태명. 불리언은 `is~`

```ts
const [userList, setUserList] = useState<SysUser[]>([]);
const [isLoading, setIsLoading] = useState(false);
const [selectedDeptCode, setSelectedDeptCode] = useState<string>();
```

### 5.8 기타 계층

**환경변수**

```
DATABASE_URL
SESSION_SECRET
NEXT_PUBLIC_APP_NAME       ← 브라우저 노출. 비밀값에 절대 붙이지 않는다
EXCEL_MAX_ROWS
```

**Git**

```
브랜치   feat/excel-upload-engine
         fix/audit-diff-null
커밋     feat: 엑셀 업로드 검증 3단계 구현
         fix: 사번 앞자리 0 유실 수정
```

type: `feat` `fix` `refactor` `chore` `docs` `test` `style`

### 5.9 앱 금지 사항

| 금지 | 사유 |
|---|---|
| 파일명 대소문자 혼용 | Linux 배포 시 import 실패 |
| `I` 접두어 인터페이스 | TS 표준 관례에 반함 |
| 타입 정보 중복 (`userNameString`) | 노이즈 |
| 부정형 불리언 (로직용) | 이중부정 유발 |
| `utils.ts` / `helpers.ts` 만능 파일 | 무한 증식. 도메인별 분리 |
| `index.ts` 남용 | 순환참조·트리셰이킹 저해 |
| 지역 const 를 UPPER_CASE | 상수는 모듈 레벨 고정값만 |
| API 경로에 동사 (`/api/getUserList`) | REST 위반 |
| `NEXT_PUBLIC_` 을 비밀값에 사용 | 클라이언트 노출 |

---

## 6. 규칙 강제 (ESLint)

**문서만으로는 지켜지지 않는다.** 특히 AI 코딩에서 그렇다. 사람은 규칙을 기억하지만 AI는 세션마다 잊는다. 린트 에러로 되돌아오는 것이 문서를 다시 읽히는 것보다 확실하다.

`eslint.config.js`

```js
{
  rules: {
    '@typescript-eslint/naming-convention': [
      'error',
      { selector: 'default',           format: ['camelCase'] },
      { selector: 'variable',          format: ['camelCase', 'UPPER_CASE', 'PascalCase'] },
      { selector: 'parameter',         format: ['camelCase'], leadingUnderscore: 'allow' },
      { selector: 'typeLike',          format: ['PascalCase'] },
      { selector: 'interface',         format: ['PascalCase'],
        custom: { regex: '^I[A-Z]', match: false } },      // I 접두어 금지
      { selector: 'enumMember',        format: ['UPPER_CASE'] },
      { selector: 'objectLiteralProperty', format: null },  // API/DB 키 예외
    ],
    'unicorn/filename-case': ['error', { case: 'kebabCase' }],
  }
}
```

추가 권장

- Prisma 스키마의 `@map` 누락 검증 스크립트
- pre-commit 훅(husky + lint-staged)으로 커밋 시점 차단

---

# PART III. 데이터 모델

## 7. 테이블 일람과 관계

| 테이블 | 구분 | 설명 |
|---|---|---|
| `sys_user` | 마스터 | 사용자 |
| `sys_dept` | 마스터 | 조직 |
| `sys_role` | 마스터 | 역할 |
| `sys_menu` | 마스터 | 메뉴 |
| `map_role_menu` | 매핑 | 역할별 메뉴권한 |
| `sys_code_group` | 마스터 | 공통코드 그룹 |
| `sys_code` | 마스터 | 공통코드 상세 |
| `log_audit` | 로그 | 변경이력 (INSERT 전용) |
| `log_login` | 로그 | 접속이력 |
| `log_excel_upload` | 로그 | 엑셀 업로드 배치 이력 |
| `his_dept` | 이력 | 조직 스냅샷 (시점 조회용) |

```
sys_dept ──1:N── sys_user ──N:1── sys_role
   │                                  │
 (self)                               │ 1:N
parent_code                     map_role_menu
                                      │ N:1
                                 sys_menu ──(self) parent_code

sys_code_group ──1:N── sys_code
sys_dept ──1:N── his_dept
```

---

## 8. 테이블 정의

### 8.1 sys_dept — 조직

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|---|---|
| dept_code | VARCHAR(20) PK | Y | 부서코드 |
| dept_name | VARCHAR(100) | Y | 부서명 |
| parent_code | VARCHAR(20) | N | 상위부서 (최상위는 NULL) |
| dept_level | INT | Y | 계층 레벨 (1부터, 자동계산) |
| sort_seq | INT | Y | 동일 레벨 내 정렬 |
| manager_emp_no | VARCHAR(20) | N | 부서장 사번 |
| use_yn | BOOLEAN | Y | 사용여부 |
| *(공통 컬럼 5개)* | | Y | 4.6 참조 |

### 8.2 sys_user — 사용자

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|---|---|
| emp_no | VARCHAR(20) PK | Y | 사번 (로그인 ID) |
| user_name | VARCHAR(100) | Y | 성명 |
| email | VARCHAR(100) UK | Y | 이메일 |
| phone | VARCHAR(20) | N | 연락처 |
| password | VARCHAR(255) | Y | bcrypt 해시 |
| dept_code | VARCHAR(20) FK | Y | → sys_dept |
| role_id | VARCHAR(20) FK | Y | → sys_role |
| position | VARCHAR(50) | N | 직위 |
| user_status | VARCHAR(20) | Y | `ACTIVE`/`INACTIVE`/`LOCKED` |
| pwd_change_yn | BOOLEAN | Y | false면 최초 로그인 시 변경 강제 |
| pwd_changed_at | TIMESTAMP | N | |
| fail_cnt | INT | Y | 로그인 실패 횟수 |
| last_login_at | TIMESTAMP | N | |
| *(공통 컬럼 5개)* | | Y | |

### 8.3 sys_role — 역할

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|---|---|
| role_id | VARCHAR(20) PK | Y | `ADMIN`/`MANAGER`/`USER` |
| role_name | VARCHAR(100) | Y | 역할명 |
| data_scope | VARCHAR(20) | Y | `ALL`/`DEPT`/`SELF` |
| role_desc | VARCHAR(200) | N | |
| use_yn | BOOLEAN | Y | |
| *(공통 컬럼 5개)* | | Y | |

- `ALL` 전사 · `DEPT` 본인부서+하위 · `SELF` 본인 작성분

### 8.4 sys_menu — 메뉴

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|---|---|
| menu_code | VARCHAR(20) PK | Y | |
| menu_name | VARCHAR(100) | Y | |
| parent_code | VARCHAR(20) | N | |
| menu_level | INT | Y | |
| sort_seq | INT | Y | |
| menu_url | VARCHAR(200) | N | 폴더성 메뉴는 NULL |
| menu_icon | VARCHAR(50) | N | |
| menu_type | VARCHAR(20) | Y | `FOLDER`/`PAGE` |
| use_yn | BOOLEAN | Y | |
| *(공통 컬럼 5개)* | | Y | |

### 8.5 map_role_menu — 역할별 메뉴권한

| 컬럼 | 타입 | 설명 |
|---|---|---|
| role_id | VARCHAR(20) PK FK | |
| menu_code | VARCHAR(20) PK FK | |
| read_yn | BOOLEAN | 조회 |
| create_yn | BOOLEAN | 등록 |
| update_yn | BOOLEAN | 수정 |
| delete_yn | BOOLEAN | 삭제 |
| export_yn | BOOLEAN | 엑셀 다운로드 |
| *(공통 컬럼 5개)* | | |

### 8.6 sys_code_group / sys_code — 공통코드

**sys_code_group**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| group_code | VARCHAR(30) PK | |
| group_name | VARCHAR(100) | |
| group_desc | VARCHAR(200) | |
| use_yn | BOOLEAN | |

**sys_code**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| group_code | VARCHAR(30) PK FK | |
| code | VARCHAR(30) PK | |
| code_name | VARCHAR(100) | |
| sort_seq | INT | |
| ref_value1 | VARCHAR(100) | 부가값 (색상, 단위 등) |
| ref_value2 | VARCHAR(100) | |
| use_yn | BOOLEAN | |

두 테이블 모두 공통 컬럼 5개 포함.

### 8.7 log_audit — 변경이력 (INSERT 전용)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| audit_id | BIGSERIAL PK | |
| table_name | VARCHAR(50) | 대상 테이블 |
| record_key | VARCHAR(200) | PK값. 복합키는 `\|` 결합 |
| action_type | VARCHAR(20) | `INSERT`/`UPDATE`/`DELETE` |
| changed_cols | TEXT[] | 실제 변경된 컬럼명 |
| before_json | JSONB | |
| after_json | JSONB | |
| changed_by | VARCHAR(20) | |
| changed_at | TIMESTAMP | |
| client_ip | VARCHAR(45) | |
| source_type | VARCHAR(20) | `SCREEN`/`EXCEL`/`BATCH`/`API` |
| batch_id | UUID | 엑셀 업로드 1회 묶음 |
| change_reason | VARCHAR(200) | 변경사유 (선택) |

```sql
CREATE INDEX ix_log_audit_target ON log_audit(table_name, record_key, changed_at DESC);
CREATE INDEX ix_log_audit_user   ON log_audit(changed_by, changed_at DESC);
CREATE INDEX ix_log_audit_batch  ON log_audit(batch_id);
```

### 8.8 his_dept — 조직 이력

`sys_dept` 와 동일 구조 + `hist_id`(BIGSERIAL PK), `change_type`(VARCHAR 20), `changed_by`, `changed_at`

### 8.9 log_login — 접속 로그

| 컬럼 | 타입 | 설명 |
|---|---|---|
| login_id | BIGSERIAL PK | |
| emp_no | VARCHAR(20) | |
| login_at | TIMESTAMP | |
| client_ip | VARCHAR(45) | |
| user_agent | VARCHAR(300) | |
| success_yn | BOOLEAN | |
| fail_reason | VARCHAR(100) | |

### 8.10 log_excel_upload — 업로드 이력

| 컬럼 | 타입 | 설명 |
|---|---|---|
| batch_id | UUID PK | log_audit.batch_id 와 연결 |
| target_entity | VARCHAR(50) | |
| file_name | VARCHAR(200) | |
| total_cnt / success_cnt / fail_cnt | INT | |
| upload_status | VARCHAR(20) | `SUCCESS`/`FAILED` |
| uploaded_by | VARCHAR(20) | |
| uploaded_at | TIMESTAMP | |

---

## 9. DDL 예시

```sql
CREATE TABLE sys_dept (
  dept_code       VARCHAR(20)   NOT NULL,
  dept_name       VARCHAR(100)  NOT NULL,
  parent_code     VARCHAR(20),
  dept_level      INT           NOT NULL DEFAULT 1,
  sort_seq        INT           NOT NULL DEFAULT 0,
  manager_emp_no  VARCHAR(20),
  use_yn          BOOLEAN       NOT NULL DEFAULT true,
  del_yn          BOOLEAN       NOT NULL DEFAULT false,
  created_by      VARCHAR(20)   NOT NULL,
  created_at      TIMESTAMP     NOT NULL DEFAULT now(),
  updated_by      VARCHAR(20),
  updated_at      TIMESTAMP,
  CONSTRAINT pk_sys_dept PRIMARY KEY (dept_code)
);

CREATE TABLE sys_user (
  emp_no          VARCHAR(20)   NOT NULL,
  user_name       VARCHAR(100)  NOT NULL,
  email           VARCHAR(100)  NOT NULL,
  phone           VARCHAR(20),
  password        VARCHAR(255)  NOT NULL,
  dept_code       VARCHAR(20)   NOT NULL,
  role_id         VARCHAR(20)   NOT NULL,
  position        VARCHAR(50),
  user_status     VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE',
  pwd_change_yn   BOOLEAN       NOT NULL DEFAULT false,
  pwd_changed_at  TIMESTAMP,
  fail_cnt        INT           NOT NULL DEFAULT 0,
  last_login_at   TIMESTAMP,
  del_yn          BOOLEAN       NOT NULL DEFAULT false,
  created_by      VARCHAR(20)   NOT NULL,
  created_at      TIMESTAMP     NOT NULL DEFAULT now(),
  updated_by      VARCHAR(20),
  updated_at      TIMESTAMP,
  CONSTRAINT pk_sys_user        PRIMARY KEY (emp_no),
  CONSTRAINT uk_sys_user_email  UNIQUE (email),
  CONSTRAINT fk_sys_user_dept   FOREIGN KEY (dept_code) REFERENCES sys_dept(dept_code),
  CONSTRAINT fk_sys_user_role   FOREIGN KEY (role_id)   REFERENCES sys_role(role_id),
  CONSTRAINT ck_sys_user_status CHECK (user_status IN ('ACTIVE','INACTIVE','LOCKED'))
);

CREATE TABLE log_audit (
  audit_id      BIGSERIAL,
  table_name    VARCHAR(50)   NOT NULL,
  record_key    VARCHAR(200)  NOT NULL,
  action_type   VARCHAR(20)   NOT NULL,
  changed_cols  TEXT[],
  before_json   JSONB,
  after_json    JSONB,
  changed_by    VARCHAR(20)   NOT NULL,
  changed_at    TIMESTAMP     NOT NULL DEFAULT now(),
  client_ip     VARCHAR(45),
  source_type   VARCHAR(20)   NOT NULL DEFAULT 'SCREEN',
  batch_id      UUID,
  change_reason VARCHAR(200),
  CONSTRAINT pk_log_audit PRIMARY KEY (audit_id)
);
```

---

# PART IV. 기능 명세

## 10. 인증

### 10.1 기능

| 기능 | 규칙 |
|---|---|
| 로그인 | 사번 + 비밀번호. `user_status != 'ACTIVE'` 이면 거부 |
| 비밀번호 저장 | bcrypt (cost 10) |
| 최초 로그인 | `pwd_change_yn = false` 이면 비밀번호 변경 화면 강제 |
| 실패 잠금 | 5회 실패 시 `user_status = 'LOCKED'`. 관리자만 해제 |
| 비밀번호 초기화 | 관리자 실행. 임시비번 + `pwd_change_yn = false` |
| 세션 | 만료 8시간. 자동 연장 없음 |

### 10.2 비밀번호 정책

- 최소 9자, 영문 + 숫자 + 특수문자 중 **2종 이상**
- 사번·이름 포함 금지
- 직전 비밀번호와 동일 금지
- 주기적 변경 강제는 미적용

---

## 11. 권한

### 11.1 구조

```
sys_user ──(1:1)──> sys_role ──(1:N)──> map_role_menu (메뉴별 CRUD)
                        └──> data_scope (ALL/DEPT/SELF)
```

사용자당 역할은 **1개**로 제한한다. 다중 역할은 권한 병합 규칙이 필요해 복잡도가 급증한다.

### 11.2 권한 체크 지점 (2중)

**① 메뉴 노출** — 로그인 시 `map_role_menu.read_yn = true` 인 메뉴만 조회해 사이드바 구성

**② API 가드** — 모든 요청에서 서버 검증. **이쪽이 실제 방어선이다.**

```
GET → read_yn    POST → create_yn    PUT → update_yn    DELETE → delete_yn
```

> 메뉴만 숨기고 API를 열어두면 URL 직접 호출로 뚫린다.

### 11.3 데이터 범위 필터

화면마다 `where` 를 직접 쓰지 말고 공통 함수로 처리한다.

```ts
function scopeFilter(user: SessionUser) {
  switch (user.dataScope) {
    case 'ALL':  return {};
    case 'DEPT': return { deptCode: { in: getDeptTree(user.deptCode) } };
    case 'SELF': return { createdBy: user.empNo };
  }
}
```

`getDeptTree()` 는 본인 부서 + 모든 하위 부서 코드를 재귀 CTE 로 반환한다.

### 11.4 기본 역할

| role_id | role_name | data_scope | 권한 |
|---|---|---|---|
| ADMIN | 시스템관리자 | ALL | 전체 메뉴 CRUD |
| MANAGER | 부서관리자 | DEPT | 업무 CRUD, 관리메뉴 조회 |
| USER | 일반사용자 | SELF | 업무 조회/등록 |

---

## 12. 변경이력

### 12.1 구현 원칙

**ORM 훅에서 자동 기록한다.** 서비스 코드에서 감사로그 함수를 직접 호출하지 않는다.

```ts
prisma.$extends({
  query: {
    $allModels: {
      async create({ model, args, query }) { /* after 기록 */ },
      async update({ model, args, query }) { /* before 조회 → diff → 기록 */ },
    }
  }
})
```

### 12.2 규칙

| 항목 | 규칙 |
|---|---|
| 대상 | sys_user, sys_dept, sys_role, map_role_menu, sys_menu, sys_code_group, sys_code |
| 트랜잭션 | 업무 트랜잭션과 **동일 트랜잭션**. 실패 시 함께 롤백 |
| UPDATE diff | 실제 값이 바뀐 컬럼만 `changed_cols` 에. 변경 없으면 로그 미생성 |
| diff 제외 | `created_at`, `updated_at`, `created_by`, `updated_by` |
| 마스킹 | `password` 는 before/after 모두 `"***"` |
| 논리삭제 | `del_yn = true` UPDATE 시 `action_type = 'DELETE'` |
| 변경자 | 세션 컨텍스트(AsyncLocalStorage)에서 자동 주입 |
| 엑셀 업로드 | `source_type = 'EXCEL'`, 동일 `batch_id` |
| 수정/삭제 | `log_audit` 은 **INSERT 전용**. UPDATE/DELETE API 미제공 |

### 12.3 조직 이력 (his_dept)

`sys_dept` 변경 시 `log_audit` 과 **별도로** `his_dept` 에 전체 스냅샷 1행 INSERT.

```sql
SELECT DISTINCT ON (dept_code) *
FROM his_dept
WHERE changed_at <= :base_date
ORDER BY dept_code, changed_at DESC;
```

### 12.4 ★ 업무 테이블 설계 규칙

향후 만들 모든 `biz_` 테이블에는 작성 시점 부서코드를 **값으로 복사 저장**한다.

```
dept_code_snapshot   VARCHAR(20)   -- 작성 당시 작성자의 부서코드
```

사용자가 부서를 옮겨도 과거 데이터가 새 부서로 따라가지 않게 하는 유일한 방법이다. 나중에 추가하면 전체 데이터 보정이 필요하므로 **처음부터 넣는다.**

### 12.5 이력 조회 화면

**① 통합 이력조회** (관리자)
- 검색: 대상 테이블 / 변경자 / 기간 / 작업구분 / 유입경로
- 상세 팝업: `changed_cols` 기준 **변경된 항목만** 좌우 비교

```
부서명    영업1팀   →   영업기획팀
부서장    김철수    →   이영희
```

- `batch_id` 클릭 시 해당 엑셀 업로드 전체 건 조회

**② 레코드별 이력 버튼** — 상세 화면 우상단 `변경이력` → 타임라인. 실사용 빈도는 이쪽이 더 높다.

### 12.6 보관

보관기간 3년, 이후 아카이브 이관. 엑셀 대량 업로드가 잦으면 월 단위 파티셔닝 고려.

---

## 13. 엑셀 일괄 업로드 엔진

### 13.1 구조

```
공통 엔진 (1개)
  ├─ 템플릿 생성기
  ├─ 파서 (SheetJS)
  ├─ 검증기 (3단계)
  ├─ 미리보기 UI
  └─ 커밋 (트랜잭션)
        ↑
   엔티티 정의 (.def.ts) — 화면마다 이것만 추가
```

### 13.2 엔티티 정의 스키마

```ts
type UploadDef = {
  entity: string;              // 'sysUser'
  label: string;               // '사용자'
  table: string;               // 'sys_user'
  keys: string[];              // UPSERT 판단 키
  mode: 'INSERT' | 'UPSERT';
  maxRows: number;             // 5000
  columns: ColumnDef[];
};

type ColumnDef = {
  header: string;              // 엑셀 헤더명 (완전일치)
  field: string;               // DB 컬럼 (snake_case)
  type: 'string' | 'number' | 'date' | 'boolean' | 'code';
  required: boolean;
  maxLength?: number;
  pattern?: RegExp;
  ref?: string;                // 'sys_dept.dept_code' — 존재 검증
  codeGroup?: string;          // 'USER_STATUS' — 공통코드 검증
  default?: unknown;
  sample: string;              // 템플릿 샘플값
};
```

### 13.3 검증 3단계

| 단계 | 내용 |
|---|---|
| 1. 형식 | 필수값, 타입, 길이, 포맷(날짜/숫자/이메일) |
| 2. 참조 무결성 | 부서코드·역할ID·공통코드 실존, 상위코드 존재, 순환참조 |
| 3. 중복 | 파일 내 키 중복, DB 기존 데이터 중복(INSERT 모드) |

### 13.4 처리 흐름

```
1. 템플릿 다운로드   [데이터] + [코드값안내] + [작성요령] 3개 시트
2. 파일 업로드       .xlsx/.xls, 10MB 이하, 헤더 완전일치 검증
3. 검증 → 미리보기   전체/정상/오류 건수, 행별 오류사유,
                     [오류내역 엑셀 다운로드]
4. 확정              오류 1건이라도 있으면 버튼 비활성.
                     전건 정상 시에만 단일 트랜잭션 커밋
5. 결과              log_excel_upload 기록,
                     log_audit 에 source_type='EXCEL' + batch_id
```

**부분 반영은 하지 않는다.** 500건 중 480건만 들어간 상태는 복구가 매우 어렵다.

### 13.5 ★ 파싱 주의사항

실무에서 가장 자주 터지는 지점이다.

```ts
XLSX.utils.sheet_to_json(sheet, { raw: false, defval: '' });
```

| 문제 | 대응 |
|---|---|
| 사번 `00123` → 숫자 `123` | `raw: false` + 문자열 유지 |
| 날짜가 시리얼 넘버(45000) | `cellDates: true` 또는 직접 변환 |
| 앞뒤 공백, 전각 공백 | 전 값 `trim()` + 전각→반각 정규화 |
| 빈 행 혼입 | 전 컬럼 공백 행은 스킵 |
| 셀 병합 | 템플릿에서 병합 금지 명시 |
| 헤더 오타/순서 변경 | 완전일치 검증, 불일치 시 거부 |

### 13.6 업로드 순서 의존성

템플릿 `[작성요령]` 시트에 명시한다.

```
① 공통코드 → ② 조직 → ③ 역할 → ④ 사용자 → ⑤ 메뉴 → ⑥ 역할별권한
```

### 13.7 대상 엔티티

| 엔티티 | 테이블 | 모드 | 키 |
|---|---|---|---|
| 공통코드 | sys_code | UPSERT | group_code + code |
| 조직 | sys_dept | UPSERT | dept_code |
| 사용자 | sys_user | UPSERT | emp_no |
| 메뉴 | sys_menu | UPSERT | menu_code |
| 역할별권한 | map_role_menu | UPSERT | role_id + menu_code |

---

# PART V. 화면과 인터페이스

## 14. 화면 목록

| 메뉴 | 화면 | 주요 기능 |
|---|---|---|
| 로그인 | 로그인 | 인증, 실패 카운트 |
| 로그인 | 비밀번호 변경 | 최초 로그인 시 강제 |
| 시스템관리 | 사용자관리 | 목록/등록/수정, 비번초기화, 잠금해제, **엑셀** |
| 시스템관리 | 조직관리 | 트리 + 상세, **엑셀** |
| 시스템관리 | 역할관리 | 목록/등록/수정 |
| 시스템관리 | 메뉴관리 | 트리 + 상세, **엑셀** |
| 시스템관리 | 권한관리 | 역할×메뉴 매트릭스, **엑셀** |
| 시스템관리 | 공통코드관리 | 그룹/상세 2단, **엑셀** |
| 시스템관리 | 변경이력조회 | 통합 조회 + 상세 비교 |
| 시스템관리 | 접속이력조회 | 로그인 성공/실패 |
| 시스템관리 | 업로드이력조회 | 엑셀 배치 이력 |
| 공통 | 마이페이지 | 본인정보, 비밀번호 변경 |

---

## 15. 공통 컴포넌트

| 컴포넌트 | 설명 |
|---|---|
| `<DataGrid>` | 검색 + 정렬 + 페이징 + 엑셀다운로드 |
| `<TreeView>` | 조직/메뉴 공용 트리 |
| `<ExcelUpload>` | 엔티티 정의 기반 공통 업로드 모달 |
| `<UserPicker>` | 사용자 검색 팝업 |
| `<DeptPicker>` | 부서 선택 팝업 |
| `<CodeSelect>` | 공통코드 그룹 지정 시 자동 옵션 로딩 |
| `<AuditHistory>` | 레코드별 변경이력 타임라인 |
| `<ConfirmDialog>` / `<Toast>` | 공통 알림 |

---

## 16. API 규격

### 16.1 경로

```
/api/sys-user                            GET(목록)  POST(등록)
/api/sys-user/[empNo]                    GET  PUT  DELETE
/api/sys-user/[empNo]/reset-password     POST
/api/excel/template/[entity]             GET
/api/excel/validate                      POST
/api/excel/commit                        POST
```

| 규칙 | 내용 |
|---|---|
| 표기 | 소문자 kebab-case |
| 명사 | 리소스는 명사. 경로에 동사를 넣지 않음 |
| 단수/복수 | **DB 테이블명 그대로 단수** (매핑 명확성 우선) |
| 동작 | CRUD 벗어나는 동작만 하위 경로에 동사 (`/reset-password`, `/unlock`) |
| 계층 | 2단계까지. 그 이상은 쿼리파라미터 |

### 16.2 쿼리 파라미터

```
/api/sys-user?deptCode=D001&userStatus=ACTIVE&keyword=홍&page=1&size=50&sort=userName,asc
```

- camelCase (JSON 키와 통일)
- 페이징 `page` / `size`, 정렬 `sort={field},{asc|desc}`, 검색어 `keyword`

### 16.3 응답 포맷

```json
{ "success": true,  "data": { ... }, "message": null }
{ "success": false, "data": null, "message": "오류 메시지", "code": "E4001" }
```

목록 응답:

```json
{
  "success": true,
  "data": {
    "content": [ ... ],
    "page": 1,
    "size": 50,
    "totalCount": 137
  }
}
```

### 16.4 에러 코드 체계

```
E{계층}{일련}
```

| 대역 | 계층 | 예 |
|---|---|---|
| E1xxx | 인증 | `E1001` 로그인 실패, `E1002` 계정 잠김, `E1003` 세션 만료 |
| E2xxx | 권한 | `E2001` 메뉴 권한 없음, `E2002` 데이터 범위 초과 |
| E3xxx | 검증 | `E3001` 필수값 누락, `E3002` 형식 오류 |
| E4xxx | 엑셀 | `E4001` 헤더 불일치, `E4002` 참조 무결성, `E4003` 중복 |
| E5xxx | 업무 로직 | |
| E9xxx | 시스템 | |

### 16.5 DTO 타입 명명

```ts
type UserListItem      // 목록 행
type UserDetail        // 상세
type UserCreateInput   // 등록 요청
type UserUpdateInput   // 수정 요청
type UserSearchParams  // 검색 조건
```

Prisma 모델(`SysUser`)과 DTO를 이름으로 구분한다. 같은 이름을 쓰면 어느 계층 타입인지 알 수 없다.

---

# PART VI. 구현

## 17. 구현 순서

| STEP | 내용 | 이유 |
|---|---|---|
| 1 | 프로젝트 셋업 + ESLint + 스키마 | 규칙을 코드로 고정 |
| 2 | **감사로그 인프라** | 이후 모든 화면에 자동 적용 |
| 3 | **엑셀 업로드 엔진** | 이후 모든 데이터를 이걸로 투입 |
| 4 | 인증 | |
| 5 | 권한 미들웨어 | |
| 6 | 관리 화면 (코드→조직→역할→사용자→메뉴→권한) | |
| 7 | 이력조회 화면 | |
| 8 | 업무 화면 | |

**2·3을 앞에 두는 것이 핵심이다.** 인프라를 먼저 깔면 이후 만드는 모든 화면이 자동으로 적용된다. 반대로 나중에 붙이면 이미 만든 화면을 전부 다시 열어야 한다.

---

## 18. 구현 프롬프트 세트

> 순서대로 입력한다. 각 STEP 완료 후 19장 체크리스트로 검증한다.
> 모든 STEP 에서 **PART II(명명 규칙) 준수**를 명시한다.

### STEP 1 — 셋업 + ESLint + 스키마

```
SPEC.md 의 [PART II 명명 규칙] 과 [PART III 데이터 모델] 을 기준으로
프로젝트를 셋업하고 Prisma 스키마를 작성해줘.

1) ESLint 설정 (6장 그대로)
   - naming-convention 규칙, I 접두어 금지
   - filename-case: kebabCase

2) 폴더 구조 (5.3 기능 단위)

3) Prisma 스키마
   - 테이블: 접두어(sys_/log_/his_/map_) + snake_case + 단수형
   - 컬럼: 표준 도메인 접미어(4.4) 준수. 접미어에 따라 타입·길이 고정
   - 불리언은 전부 _yn. is_ 접두어 금지
   - 공통 컬럼 5개(del_yn, created_by, created_at, updated_by, updated_at)를
     모든 마스터 테이블에 동일한 이름·순서로.
     단 log_* 테이블에는 넣지 말 것
   - FK 컬럼명은 참조 대상 PK 컬럼명과 동일하게
   - 제약조건: pk_/fk_/uk_/ix_/ck_
   - 표준 단어사전(부록 A)에 없는 축약어를 만들지 말 것
   - @map/@@map 으로 매핑. TS 필드는 camelCase
   - 7장 11개 테이블 전부 + 8.7 인덱스

4) 시드 스크립트
   - 역할 3개, 최상위 부서 1개, 관리자 계정 1개, 기본 메뉴 트리,
     공통코드 그룹(부록 B)

다음 STEP 에서 감사로그 훅을 붙일 예정이니 그 구조를 염두에 둘 것.
```

### STEP 2 — 감사로그 인프라 ★

```
SPEC.md 의 [12. 변경이력] 을 구현해줘. 이 프로젝트의 핵심 인프라야.

- Prisma Client Extension($extends)으로 create/update/updateMany 를 가로채
  log_audit 을 자동 기록. 서비스 코드에서 직접 호출하지 않게 할 것
- UPDATE 시 before 조회 → diff → 실제 변경된 컬럼만 changed_cols 에.
  변경사항 없으면 로그를 만들지 말 것
- created_at/updated_at/created_by/updated_by 는 diff 제외
- password 는 before_json/after_json 모두 "***" 마스킹
- del_yn=true 로의 UPDATE 는 action_type='DELETE'
- changed_by, client_ip, source_type, batch_id 는 AsyncLocalStorage
  컨텍스트에서 자동 주입. 요청 미들웨어에서 컨텍스트 세팅
- 업무 트랜잭션과 동일 트랜잭션에서 기록
- sys_dept 는 log_audit 과 별도로 his_dept 에 전체 스냅샷도 INSERT

동작 확인용 테스트 코드도 함께 작성해줘.
```

### STEP 3 — 엑셀 업로드 공통 엔진 ★

```
SPEC.md 의 [13. 엑셀 일괄 업로드 엔진] 을 구현해줘.
화면별로 만들지 말고 재사용 가능한 공통 엔진으로.

1. UploadDef 타입 정의 (13.2 그대로)
2. 템플릿 생성기 — [데이터]+[코드값안내]+[작성요령] 3개 시트
3. 파서 — SheetJS. 반드시 raw:false.
   13.5 주의사항 전부 반영 (사번 앞자리 0, 날짜 시리얼, 공백, 빈행, 헤더검증)
4. 검증기 — 3단계. 행별 오류사유 배열 반환
5. 오류 엑셀 생성기 — 원본 + '오류사유' 컬럼
6. 커밋 — 전건 정상일 때만 단일 트랜잭션.
   batch_id 생성 후 AsyncLocalStorage 에 세팅해 log_audit 과 연결.
   log_excel_upload 기록
7. <ExcelUpload> 공통 UI — 업로드 → 미리보기 → 확정.
   오류 1건이라도 있으면 확정 버튼 비활성

파일은 features/excel-upload/ 아래, 엔티티 정의는 defs/*.def.ts 로.
먼저 sys_code 엔티티 정의로 end-to-end 동작시켜 검증해줘.
```

### STEP 4 — 인증

```
SPEC.md 의 [10. 인증] 을 구현해줘.

- 로그인/로그아웃, bcrypt(cost 10)
- user_status != 'ACTIVE' 이면 거부 (사유별 메시지 구분)
- 실패 5회 시 LOCKED 전환 (fail_cnt 증가)
- pwd_change_yn=false 이면 비밀번호 변경 화면으로 강제 리다이렉트
- 비밀번호 정책 검증 (10.2)
- 세션 8시간, 자동연장 없음
- 모든 로그인 시도를 log_login 에 기록
- 로그인 성공 시 AsyncLocalStorage 컨텍스트에 사용자 정보 세팅
- 에러코드는 16.4 의 E1xxx 대역 사용
```

### STEP 5 — 권한 미들웨어

```
SPEC.md 의 [11. 권한] 을 구현해줘.

- 로그인 시 map_role_menu.read_yn=true 인 메뉴만 조회해 사이드바 트리 구성
- API 미들웨어: 요청 경로 → menu_code 매핑 후 메서드별 검증
  (GET→read_yn, POST→create_yn, PUT→update_yn, DELETE→delete_yn)
- scopeFilter(user) 공통 함수 (11.3). getDeptTree 는 재귀 CTE
- 권한 없음 시 403 + E2xxx 에러코드

메뉴만 숨기는 게 아니라 API 레벨에서 반드시 막아야 해.
```

### STEP 6 — 관리 화면

```
SPEC.md 의 [14. 화면 목록] 중 시스템관리 화면을 구현해줘.
순서: 공통코드 → 조직 → 역할 → 사용자 → 메뉴 → 권한

공통:
- [15. 공통 컴포넌트] 먼저 구축
- 모든 목록에 검색/정렬/페이징/엑셀다운로드
- 모든 상세 화면 우상단에 [변경이력] 버튼 → <AuditHistory>
- 각 화면에 STEP 3 의 <ExcelUpload> 연결 (엔티티 정의만 추가)
- 삭제는 del_yn 논리삭제. remove~ 함수명 사용
- API 경로 kebab-case, JSON 키 camelCase, DTO 명명은 16.5
- 폴더는 features/{도메인}/ 아래 기능 단위로

특이사항:
- 조직/메뉴: 좌측 트리 + 우측 상세 2분할
- 권한관리: 역할 선택 → 메뉴 트리 × CRUD 체크박스 매트릭스,
  상위 메뉴 체크 시 하위 일괄 적용
```

### STEP 7 — 이력조회 화면

```
SPEC.md 의 [12.5] 를 구현해줘.

- 통합 이력조회: 테이블/변경자/기간/작업구분/유입경로 검색
- 상세 팝업: changed_cols 기준 변경된 항목만 좌우 비교
- batch_id 클릭 시 해당 엑셀 업로드 전체 건 조회
- 접속이력조회(log_login), 업로드이력조회(log_excel_upload)
- log_audit 은 조회 전용. 수정/삭제 API 를 만들지 말 것
```

---

## 19. 완료 체크리스트

### 19.1 명명 규칙 — DB
- [ ] 예약어(user, role, code)가 접두어 없이 쓰인 테이블이 없다
- [ ] 불리언 컬럼이 전부 `_yn` 이다 (`is_` 없음)
- [ ] 같은 접미어 컬럼이 테이블마다 다른 타입·길이로 정의되지 않았다
- [ ] 공통 컬럼 5개가 모든 마스터 테이블에 동일한 이름으로 있다
- [ ] `log_*` 테이블에 공통 컬럼이 들어가지 않았다
- [ ] FK 컬럼명이 참조 대상 PK 컬럼명과 같다
- [ ] 제약조건에 `pk_/fk_/uk_/ix_/ck_` 명명이 적용됐다
- [ ] 표준 단어사전에 없는 축약어가 없다

### 19.2 명명 규칙 — 애플리케이션
- [ ] `npm run lint` 가 통과한다
- [ ] 파일·폴더가 전부 kebab-case 다
- [ ] `I` 접두어 인터페이스가 없다
- [ ] DB 유래 불리언이 `~Yn` 으로 유지됐다 (`isUsed` 로 개명 안 됨)
- [ ] 물리삭제 함수(`delete~`)가 없다
- [ ] `utils.ts` / `helpers.ts` 만능 파일이 없다
- [ ] API 경로에 동사가 없다

### 19.3 감사로그
- [ ] 서비스 코드에 감사로그 직접 호출이 **한 줄도 없다**
- [ ] 수정 시 변경된 컬럼만 `changed_cols` 에 기록된다
- [ ] 값을 바꾸지 않고 저장하면 로그가 생기지 않는다
- [ ] `password` 가 평문으로 남지 않는다
- [ ] 삭제 시 `action_type='DELETE'` 로 남고 데이터는 유지된다
- [ ] 트랜잭션 롤백 시 감사로그도 함께 롤백된다
- [ ] `sys_dept` 수정 시 `his_dept` 에도 스냅샷이 남는다

### 19.4 엑셀
- [ ] 사번 `00123` 이 `123` 으로 변형되지 않는다
- [ ] 날짜가 시리얼 넘버로 들어가지 않는다
- [ ] 헤더를 바꾼 파일은 업로드가 거부된다
- [ ] 오류 1건 포함 시 전체가 반영되지 않는다
- [ ] 오류내역 엑셀에 사유가 정확히 표시된다
- [ ] 업로드 건들이 동일 `batch_id` 로 묶여 조회된다
- [ ] 새 엔티티 추가 시 **정의 파일만 작성**하면 동작한다

### 19.5 권한·인증
- [ ] USER 계정으로 관리 메뉴 URL 직접 입력 시 403
- [ ] API 를 직접 호출해도 403
- [ ] `data_scope='DEPT'` 계정이 타 부서 데이터를 못 본다
- [ ] 5회 실패 시 잠기고 관리자만 해제 가능
- [ ] 초기 비밀번호로 로그인 시 변경 화면을 우회할 수 없다
- [ ] `INACTIVE` 계정은 로그인되지 않는다

---

# 부록

## 부록 A. 표준 단어사전

**등재된 단어만 사용한다.** 신규 단어는 이 표에 추가한 뒤 쓴다.

| 한글 | 사용값 | 한글 | 사용값 |
|---|---|---|---|
| 사용자 | `user` | 이력 | `hist` |
| 사번 | `emp_no` | 로그 | `log` |
| 부서/조직 | `dept` | 상태 | `status` |
| 역할 | `role` | 구분 | `type` |
| 권한 | `auth` | 순서 | `seq` |
| 메뉴 | `menu` | 사용여부 | `use_yn` |
| 코드 | `code` | 삭제여부 | `del_yn` |
| 그룹 | `group` | 등록자 | `created_by` |
| 상위 | `parent` | 수정자 | `updated_by` |
| 하위 | `child` | 비밀번호 | `password` |
| 이메일 | `email` | 연락처 | `phone` |
| 직위 | `position` | 관리자 | `manager` |
| 첨부파일 | `attach` | 제목 | `title` |
| 내용 | `content` | 비고 | `remark` |
| 시작 | `start` | 종료 | `end` |
| 기준 | `base` | 요청 | `req` |
| 승인 | `approve` | 반려 | `reject` |
| 일괄처리 | `batch` | 대상 | `target` |
| 건수 | `cnt` | 금액 | `amt` |
| 수량 | `qty` | 비율 | `rate` |

**금지 축약** — `usr`, `dpt`, `mng`, `cd`, `nm`, `dt` 등 모음을 뺀 형태

## 부록 B. 공통코드 초기값

| group_code | group_name | code | code_name |
|---|---|---|---|
| USER_STATUS | 사용자상태 | ACTIVE | 사용 |
| | | INACTIVE | 미사용 |
| | | LOCKED | 잠김 |
| DATA_SCOPE | 데이터범위 | ALL | 전사 |
| | | DEPT | 부서 |
| | | SELF | 본인 |
| MENU_TYPE | 메뉴유형 | FOLDER | 폴더 |
| | | PAGE | 화면 |
| ACTION_TYPE | 작업구분 | INSERT | 등록 |
| | | UPDATE | 수정 |
| | | DELETE | 삭제 |
| SOURCE_TYPE | 유입경로 | SCREEN | 화면 |
| | | EXCEL | 엑셀 |
| | | BATCH | 배치 |
| | | API | API |
| YN | 여부 | Y | 예 |
| | | N | 아니오 |

## 부록 C. 참고 자료

- Google TypeScript Style Guide — https://google.github.io/styleguide/tsguide.html
- typescript-eslint naming-convention — https://typescript-eslint.io/rules/naming-convention/
- TypeScript Deep Dive Style Guide (basarat) — https://github.com/basarat/typescript-book
- AWS Prescriptive Guidance: TypeScript best practices
- Next.js App Router 파일 규약 공식 문서
- 공공 데이터베이스 표준화 지침 / DA 가이드 (한국데이터산업진흥원)
