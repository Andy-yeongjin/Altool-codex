> 지침: 모든 내용은 반드시 한글로 작성합니다 (헌법 제0조).

# {기능명} — 상세 설계서 (Design)

> **요약**: {설계 핵심을 한 줄로}
>
> **프로젝트**: {프로젝트명} | **작성일**: YYYY-MM-DD | **작성**: Altool
> **상태**: Draft <!-- report 완료 시 Finalized -->
> **선행 문서**: [{기능명}.plan.md](../../01-plan/features/{기능명}.plan.md)

---

## Context Anchor

> Plan에서 복사 — Design→Run 인수인계에서 전략적 맥락이 끊기지 않게 한다.

| Key | Value |
|-----|-------|
| **WHY** | {Plan에서 복사} |
| **WHO** | {Plan에서 복사} |
| **RISK** | {Plan에서 복사} |
| **SUCCESS** | {Plan에서 복사} |
| **SCOPE** | {Plan에서 복사} |

## Design Anchor (디자인 토큰 잠금)

> `designs/design-tokens.css`가 시각 값의 원천이다. 이 기능에서 사용할 핵심 토큰을 미리 잠근다.
> 토큰에 없는 값이 필요하면 §6 "추가 토큰"에 명시하고 design-tokens.css에 추가 후 사용한다 (헌법 제16조).

| 분류 | 핵심 토큰 |
|------|----------|
| **색상** | {예: 주조 `--color-primary-600`, 상태별 `--color-success` …} |
| **타이포** | {예: `--font-sans`, 제목 `--text-2xl`} |
| **간격·크기** | {예: 카드 패딩 `--space-8`, 버튼 `--btn-height-lg`(48px ≥ 44px)} |
| **둥글기·그림자** | {예: `--radius-lg`, `--shadow-md`} |

---

## 1. 설계 목표·원칙

- {기술 목표 1 — 예: 도메인 로직을 프레임워크와 격리 (헌법 제3조)}
- {원칙 — 예: 응답 계약을 단일 타입 파일로 공유해 Contract 갭 원천 차단}

---

## 2. 아키텍처

### 2.1 아키텍처 3안 비교 (Checkpoint 3)

| 기준 | A: 최소 변경 | B: Clean | C: 실용 균형 |
|------|:-:|:-:|:-:|
| 접근 | {기존 최대 재사용} | {완전한 관심사 분리} | {적절한 경계, 과잉 없음} |
| 신규 파일 | {N} | {N} | {N} |
| 복잡도 / 유지보수성 | 낮음 / 낮음 | 높음 / 높음 | 중간 / 높음 |
| 리스크 | {결합} | {과잉 설계} | **낮음 (권장 기본값)** |

**선택**: Option {A/B/C} — **사유**: {선택 근거. 자동 파이프라인이면 "Option C 자동 선택" 명기}

### 2.2 컴포넌트 다이어그램

```
{이 기능의 실제 구조를 그린다}
페이지 ─▶ 컴포넌트('use client') ─▶ 훅 ─▶ 도메인 모듈(lib/) ─▶ {저장소/API}
```

### 2.3 데이터 흐름

```
{입력 → 검증 → 로직 → 저장 → 응답 → 화면 갱신 순으로}
```

---

## 3. 데이터 모델

### 3.1 타입 정의 (TypeScript Strict, `any` 금지 — 헌법 제6조)

```typescript
// 보편 언어 (헌법 제2조) — 기획·코드·DB 동일 용어
interface {엔티티} {
  id: string;
}
```

### 3.2 DB 스키마 (해당 시)

```
{Prisma/Drizzle 스키마 또는 localStorage 키 구조. DB 없으면 "N/A — 저장 방식: {…}"}
```

---

## 4. API 명세 (해당 시 — analyze의 Contract 검증 기준)

> API가 없으면 "N/A — Contract 축은 Match Rate에서 제외"로 표기하고 §5로.
> 응답 표준: 성공 `{ "data": … }` / 실패 `{ "error": { "code", "message" } }` — 코드·메시지는 서버·클라이언트가 **같은 타입 파일을 공유**한다.

### 4.1 엔드포인트 목록

| Method | Path | 설명 | 인증 |
|--------|------|------|------|
| | | | |

### 4.2 상세 명세

#### `{METHOD} {path}`
- 요청: `{ … }`
- **{2xx}**: `{ "data": … }`
- **400** ({조건}): `{ "error": { "code": "VALIDATION_ERROR", "message": "{한국어 사용자 안내}" } }`
- **404 / 500**: {조건과 메시지 — 사용자 안내와 개발자 로그 분리 (헌법 제13조·제14조)}

---

## 5. UI/UX 설계

### 5.1 화면 레이아웃

```
{ASCII 레이아웃 — 실제 배치 기준. .pen/Stitch가 있으면 그 구조를 따른다 (헌법 제17조)}
```

### 5.2 사용자 흐름

```
{진입 → 행동 → 결과 흐름}
```

### 5.3 컴포넌트 목록

| 컴포넌트 | 위치 | 책임 |
|----------|------|------|
| | src/… | |

### 5.4 Page UI Checklist ⚠️ CRITICAL

> **analyze의 Functional 검증 분모.** 페이지별 필수 UI 요소를 빠짐없이, 구체적인 값(필드명·옵션·토큰·aria)까지 항목화한다.
> 이 체크리스트가 부실하면 갭 분석이 "파일 존재"만 검사하게 된다.
> analyze에서 검증 통과한 항목만 `- [x]`로 갱신한다 (문서 동기화 규칙).

#### {페이지 1}

- [ ] {요소 유형}: {설명 (구체적 필드·옵션·토큰·≥44px·aria 속성)}
- [ ] {상태}: 로딩 표시 후 데이터로 교체 (영구 스켈레톤 금지)
- [ ] {상태}: 빈 데이터 안내 문구
- [ ] {상태}: 오류 시 `role="alert"` 한국어 안내 (헌법 제13조)

---

## 6. 추가 토큰

> design-tokens.css에 없어서 이 기능에서 새로 추가하는 토큰. 없으면 "없음".

| 토큰 | 값 | 용도 |
|------|-----|------|
| | | |

---

## 7. 에러 처리·보안

| 상황 | 서버/도메인 처리 | 사용자 표시 |
|------|----------------|------------|
| {검증 실패 등} | {로그 — JSON 구조화, 민감정보 제외 (제14조)} | {한국어 안내} |

- [ ] 입력 검증 (XSS·Injection 차단)
- [ ] 비밀 정보 하드코딩 없음, `.env` 격리 (제11조)
- [ ] 인증 필요 여부: {필요(방식) / 불필요(사유 — ADR로 기록)}

---

## 8. 테스트 플랜 (analyze의 Runtime 검증 기준)

> 시나리오는 여기서 정의하고, 테스트 코드는 run에서 구현과 한 세트로 작성하며(헌법 제8조), analyze가 실행한다.

### 8.1 L1 — API 시나리오 (curl) {API 없으면 "N/A"}

| # | Endpoint | Method | 테스트 | 기대 Status | 기대 응답 |
|---|----------|--------|--------|:----------:|----------|
| 1 | | | 정상 케이스 | | |
| 2 | | | 검증 실패 케이스 | 400 | `.error.code` |
| 3 | | | 없는 리소스 | 404 | |

### 8.2 L2 — UI 액션 시나리오 (Playwright)

| # | 페이지 | 액션 | 기대 결과 |
|---|--------|------|----------|
| 1 | | 로드 | §5.4 요소 전부 표시 |
| 2 | | {핵심 액션} | {상태 변화 + 데이터 검증} |

### 8.3 L3 — E2E 여정 시나리오

| # | 시나리오 | 단계 | 성공 기준 |
|---|----------|------|----------|
| 1 | {수명주기 여정} | {진입→행동→새로고침(영속 확인)→…} | 전 단계 실데이터 렌더 |

### 8.4 시드 데이터

{필요한 최소 데이터 또는 "불필요 — 빈 상태가 정상 초기값(빈 안내가 검증 대상)"}

---

## 9. 클린 아키텍처

### 9.1 레이어 배치

| 레이어 | 책임 | 위치 |
|--------|------|------|
| Presentation | 페이지·컴포넌트·훅 | src/app/, src/components/, src/hooks/ |
| Domain | 비즈니스 규칙 (프레임워크 비의존 — 헌법 제3조) | src/lib/{도메인}.ts |
| Infrastructure | DB·외부 API·브라우저 API 격리 | src/lib/{어댑터}.ts |

### 9.2 의존 규칙 (analyze가 import를 검사한다)

```
Presentation ─▶ Domain ─▶ (없음)        ← Domain은 React·브라우저 API import 금지
Infrastructure ─▶ Domain                ← 외부 세계 접근은 Infrastructure 파일에만
```

---

## 10. 구현 가이드

### 10.1 파일 구조

```
{생성·수정할 파일의 전체 트리 — analyze의 Structural 검증 분모}
```

### 10.2 구현 순서 (Depth-First — 배치당 3~5파일 완전 구현)

> run 완료 시 완료 항목을 `- [x]`로 갱신한다.

1. [ ] 배치 1: {도메인·타입 먼저 + 단위 검증}
2. [ ] 배치 2: {API + L1 즉시 검증}
3. [ ] 배치 3: {UI + 페이지 통합 + 빌드 검증}

### 10.3 세션 가이드 (Module Map)

| 모듈 | Scope Key | 내용 | 예상 규모 |
|------|-----------|------|:--------:|
| {모듈명} | `module-1` | | |

{단일 세션으로 충분하면 "단일 세션 — 분할 불필요"}

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | YYYY-MM-DD | 최초 작성 — Option {X} 선택 | Altool |
