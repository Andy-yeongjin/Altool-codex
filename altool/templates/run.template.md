> 지침: 모든 내용은 반드시 한글로 작성합니다 (헌법 제0조).
> 이 템플릿은 **run 단계가 구현 시작 전 대화에 출력하는 구현 가이드의 구조**다. 파일로 저장하지 않아도 되지만, 세션 분할 시(`--scope`)에는 저장을 권장한다.
> **자동 파이프라인 모드에서는 전체 출력을 생략**하고 핵심(결정·SC 수·배치 수) 1~2줄 요약만 출력한다.

# {기능명} — 구현 가이드 (Run)

> **선행 문서**: [{기능명}.spec.md](../02-spec/features/{기능명}.spec.md)
> **세션 범위**: {--scope 값 또는 "전체"}

---

## 📌 Context Anchor (Spec에서 복사)

| Key | Value |
|-----|-------|
| **WHY** | |
| **WHO** | |
| **RISK** | |
| **SUCCESS** | |
| **SCOPE** | |

## 📋 Decision Record Chain

> 상류 문서의 핵심 결정 — 구현이 이 결정들을 따라야 한다. 이탈이 필요하면 **문서를 먼저 수정**한다 (헌법 제9조).

| 출처 | 결정 | 근거 |
|------|------|------|
| [Plan] | {레벨·기술 결정} | |
| [Spec] | {아키텍처 Option, 상태 관리 등} | |

## 🎯 Success Criteria 추적

> Plan §4에서 복사. 구현 중 각 기준이 어디서 충족되는지 의식한다.

| # | 기준 | 담당 배치 | 상태 |
|---|------|:--------:|:----:|
| SC-1 | | 배치 {N} | ☐ |

---

## 1. 구현 전 확인

- [ ] Spec 문서 **전체**를 읽었다 (요약 금지 — 완전한 컨텍스트로 시작)
- [ ] lesson index 코드 오류 검색 — 관련 이벤트: {E-NNNNN(태그) 또는 "해당 없음"}
- [ ] Altool 자산 확인: constitution.md {✅/—} · user design source {✅/—} · design.md {✅/—}
- [ ] Spec의 User Design Source와 Design System Source를 읽고 구현할 시각 기준, 보조 기준, 복제 금지 항목을 확인했다
- [ ] 의존성 설치 상태 / 환경 변수 준비

## 2. Depth-First 구현 규칙 ⚠️ CRITICAL

> **깊게 구현하라, 넓게 펼치지 마라.**
> 안티패턴: 20개 파일 스켈레톤 → 각 30% 완성 (구조는 90%인데 기능은 빈껍데기)
> 올바른 패턴: **배치당 3~5개 파일을 100% 구현** → 검증 → 다음 배치

배치 사이클:
```
① 하위 레이어(타입·도메인) 구현 → ② 즉시 검증(단위/curl)
③ 상위 레이어(훅·컴포넌트·페이지) 구현 → ④ 즉시 검증(L2/렌더)
⑤ 전부 green → 다음 배치
```

파일별 완성 기준 (다음 파일로 넘어가기 전 확인):
- `// TODO`·플레이스홀더 없음, `console.log` 스텁 핸들러 없음
- 하드코딩 목 배열 없음 (실데이터 또는 빈 상태 처리)
- Spec §5.4의 해당 요소 전부 존재 + 실제 핸들러 연결
- 로딩·에러 상태 구현

## 3. 작업 파일 목록 (Spec §10.1 기준)

| 파일 | 신규/수정 | 배치 | 상태 |
|------|:--------:|:----:|:----:|
| | | | ☐ |

## 4. 코드-설계 추적 주석 규약

```typescript
// Spec Ref: §{절} — {이 파일이 구현하는 명세 결정}       ← 파일 상단
// Plan SC: {대응하는 성공 기준}                          ← 핵심 로직 앞
```

## 5. 항상 지킬 것 (Altool 자산 감지 시 강제)

| 항목 | 규칙 |
|------|------|
| 디자인 입력/디자인 시스템 | Spec의 User Design Source가 있으면 화면 구조와 시각 기준 1순위. `designs/design.md`를 읽고 색상·타이포·폰트 스택·간격·둥글기·그림자·컴포넌트·미디어 규칙을 추적 가능하게 구현. CSS 변수는 코드 안에서 파생해도 되지만 값의 근거는 `design.md`에 있어야 한다. 참조 폰트 파일을 복제하지 말고 `design.md`에 잠긴 구현 font-family stack을 그대로 사용한다. 파일이 없거나 비어 있거나 첫 non-empty line에 `TBD`가 있으면 Research/design_source로 돌아가 생성 후 진행. 참조 브랜드의 로고·카피·식별 레이아웃은 복제하지 않고, 근거 없는 generic AI/SaaS 기본 미감도 만들지 않음 |
| 보안 | API 키 하드코딩 금지, 입력 검증, `.env` 격리 (제6조·제11조) |
| 접근성 | 시맨틱 마크업, 충분한 조작 영역, 키보드 사용 가능, 명확한 포커스와 상태 표현 (제16조) |
| 예외 처리 | 사용자 안내(한국어)와 개발자 로그(JSON 구조화) 분리 (제13조·제14조) |

## 6. 완료 게이트 (이것 없이 "완료" 보고 금지)

- [ ] 프로덕션 빌드 성공 (`npm run build` — 헌법 제10조)
- [ ] 테스트 통과 (단위 검증 스크립트 / 기존 테스트 회귀 — 헌법 제8조)
- [ ] 문서 동기화: plan §2.1·§3.1 Status, spec §10.2 체크 갱신
- [ ] 서버는 직접 실행하지 않고 실행 방법 안내 (start.bat 또는 npm run dev)
- [ ] 코드·타입·빌드·테스트·런타임 오류가 있으면 lesson 이벤트 기록 완료 (E-NNNNN 또는 "없음")
- [ ] `.altool/checks/{기능명}.run.json` 작성 후 `check.py validate` 통과

→ 완료 후: `$altool analyze`

