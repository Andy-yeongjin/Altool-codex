# 사내 목업 5종 1차 검증

검증 상태와 문서 분류는 [문서 색인](../../verification-status.md)을 따른다.

## CI 캐시 수정 · 2026-09-11

- 변경 전 인앱 화면에는 사각 배경의 예전 CI, 원본/HTTP 응답에는 투명 녹색 CI가 존재했다.
- 공통 companyFrame의 이미지 URL에 원본 SHA-256 앞 16자리 버전을 붙이고 목업 runtime URL을 갱신했다. 회사 설정과 CI 도형/팔레트는 변경하지 않았다.
- 노랑 시안의 실제 이미지 URL은 `altool-wordmark-inverse.svg?v=4d37b30b7ccf54fd`, 로드 성공 및 높이 34px 확인. 데스크톱과 390×844 모바일 캡처에서 녹색 심볼/투명 배경을 확인했고 모바일 메뉴 열기·닫기가 동작했다.
- 생성물 일치, JS 구문, 공통 프레임 테스트 2건, 자산 팩 qa.36 및 표준 검사, diff 공백 검사 통과. 이번 검증은 CI 갱신 범위이며 전체 목업 회귀/oneshot 완료를 뜻하지 않는다.

사이드바 후속 검증: 1280px에서 시스템 이름만 있는 헤더/좌측 210px 메뉴/본문 조합을 캡처 확인했다. 390px에서 업무 메뉴 펼침 → 구매 신청 클릭 → 페이지 이동·aria-current 확인, 메뉴 다시 접힘, document scrollWidth=390px. 공통 외형은 ui-sidebar 그대로 사용하며 기존 헤더 구분선 전용 CSS는 제거했다.

- node --check: 공유 JS·생성기 구문 확인. build_company_design.py --check 생성 일치 확인.
- 인앱 HTTP: 5개 페이지 내비게이션 확인. 모바일 390px에서 각 document.scrollWidth=390; 표는 독립 스크롤.
- 경비: 회의 검색 → 1건, 체크 → 선택 처리 확인 → 완료 상태.
- 구매: 빈 신청 → 필수 오류 4개와 첫 입력 초점. 제목/희망일/금액/사유 입력 → 공통 확인 대화상자 → 모의 접수 메시지. 실제 영속 저장은 의도적으로 없음.
- 상세: 승인 확인 → 승인 상태·이력 추가.
- 대시보드: 합계 6000/3600/2400만원, 차트 집행 800+1600+1200 및 잔액 700+900+800 일치. 모바일 캡처 확인.
- 연계: 두 번째 로그 → DEMO_TIMEOUT 확인 → 재처리 → 정상 상태. 검토 상태 오류 선택 → 공통 오류 안내.

미검증: 네이티브 파일 선택·첨부 삭제, 모든 버튼/키보드/상태 조합, 브라우저 콘솔 전수 확인. 목업 시각 검토용 1차 결과이며 제품/oneshot 품질 게이트 전체 PASS가 아니다. 공통 원본은 이번 목업 제작에서 변경하지 않았다.
# 모바일 상단 메뉴 시안 검증

- 5개 목업에만 적용: 760px 이하 시스템 이름 아래 구분선 + 항상 보이는 가로 스크롤 메뉴. 공통 팩은 변경하지 않음.
- JS 구문 검사, HTML 5종 생성, diff 공백 검사 통과.
- 인앱 390px에서 5개 페이지 링크를 실제 클릭하여 목적지 제목·현재 페이지 표시·문서 가로 넘침 없음 확인. 메뉴 아이템 높이 44px, 현재 항목 자동 노출 확인.
- 652px 가로 메뉴/넘침 없음, 1280px 210px 사이드바 + 1070px 본문/세로 메뉴/넘침 없음 확인.
- 초기 검증에서 이전 CSS 캐시와 공통 선택자 우선순위로 메뉴 폭이 늘어난 문제를 발견해 시안 CSS 및 자산 URL 버전을 수정한 뒤 재검증했다.
- 이번 검증은 탐색 변경 범위이며 전체 폼·상태·콘솔/네트워크 회귀 완료를 의미하지 않는다.
# 공통 반응형 탐색 승격 검증

- 시안 전용 메뉴 CSS/JS를 공통 components.css 및 CompanyBusiness.responsiveNavigation으로 이동하고 목업 5개에서 재사용한다. V27-N01/사용 API/페이지 조합 예제 동기화.
- 배포 회귀 검사: 공통 런타임 menu SVG 내장, API export, 배포 CSS 그림자, 목업 중복 구현 제거 확인.
- 인앱 390px: 공통 아이콘 존재, 헤더→목록 간격 6px, 그림자 rgba(24,51,79,.56) 0 8px 20px, 문서 넘침 없음 실측. 메뉴 열기→경비 내역 조회 링크→제목 확인→바깥 클릭 닫힘 통과.
- 1280px 전환: 동일 메뉴 DOM이 사이드바로 복귀, 펼침 상태, 열 너비 210px/1070px 확인.
- 처음 배포 검증에서 menu 아이콘 누락을 발견하여 번들 내장 목록에 추가하고 재검증했다. 전체 페이지 업무/모든 키보드 경로 검증 완료를 의미하지 않는다.
# 회사 설정·CI·데스크톱 토글·푸터 공통화

- design/company.json을 단일 원본으로 등록하고 제작 시 business.js에 내장한다. CI 경로 실재/brand 경계, 회사명, 링크 스킴을 제작기에서 검사한다. 목업 HTML의 회사명/연락처/CI 경로 복제와 목업 CSS·토글 로직을 제거하고 companyFrame/responsiveNavigation을 소비한다.
- 1280px 인앱 실측: CI34px 및 로드, 토글 아이콘16px, 접힘 본문1280px→복원210px/1070px. 이용 안내/문의 팝업 열기·닫기 통과.
- 390px: 데스크톱 토글 숨김, 모바일 메뉴로 결재 상세 이동, CI alt Altool, 설정 기반 푸터 및 예시 표시, footer column 배치와 문서 넘침 없음 확인.
- 단위 검사 2건: 공통 배포 아이콘/API/CSS, 설정→번들 값 동일, CI 파일 실재, 목업의 회사 정보와 토글 중복 제거 확인.
- 이번 검증은 회사 프레임 변경 범위다. 모든 업무 기능·모든 키보드/콘솔·네트워크 경로의 전체 회귀 통과를 의미하지 않는다.
